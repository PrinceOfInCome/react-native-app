/**
 * @format
 */
import React from 'react';
import {AppRegistry} from 'react-native';
import Mains from './src/Main';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Mains);
